<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BlissApp.Pages.Callback.CallbackDetailPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:BlissApp.DTO.CallBackModule;assembly=BlissApp.DTO"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewmodel="clr-namespace:BlissApp.ViewModels"
    Title="Callback History"
    x:DataType="viewmodel:CallbackDetailsViewModel"
    Background="{StaticResource BackgroudColor}"
    Shell.BackgroundColor="White"
    Shell.ForegroundColor="Black"
    Shell.TitleColor="Black">
    <ScrollView>

        <Grid
            ColumnDefinitions="*,*"
            ColumnSpacing="5"
            RowDefinitions="*,Auto"
            RowSpacing="0">

            <RefreshView
                Grid.ColumnSpan="2"
                Command="{Binding GetHistoryCommand}"
                IsRefreshing="{Binding IsRefreshing}">

                <CollectionView ItemsSource="{Binding listofCallbackHistory}" SelectionMode="None">

                    <CollectionView.EmptyView>
                        <ContentView>
                            <StackLayout HorizontalOptions="Center" VerticalOptions="Center">

                                <!--<Label     Text="There is no data to show !"
                                         Margin="10,25,10,10"
                                         FontAttributes="Bold"
                                         FontSize="18"
                                         HorizontalOptions="Fill"
                                         HorizontalTextAlignment="Center" />

                                <Label     Text="Try again later."
                                         FontAttributes="Italic"
                                         FontSize="12"
                                         HorizontalOptions="Fill"
                                         HorizontalTextAlignment="Center" />-->


                                <Image Margin="50" Source="empty.svg" />
                            </StackLayout>
                        </ContentView>
                    </CollectionView.EmptyView>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="model:CallBackDTO">
                            <Grid Margin="10">
                                <Frame Style="{StaticResource CardView}">
                                    <Grid Padding="15">
                                        <VerticalStackLayout>
                                            <Label Style="{StaticResource LabelMedium}" Text="{Binding CallBackReasons}" />

                                            <Grid ColumnSpacing="5" RowSpacing="1">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition />
                                                </Grid.ColumnDefinitions>
                                                <Label
                                                    Grid.Row="0"
                                                    Grid.Column="0"
                                                    FontAttributes="Italic"
                                                    Style="{StaticResource LabelMedium}"
                                                    Text="{Binding NewCreateDate}" />
                                                <Label
                                                    x:Name="label"
                                                    Grid.Row="0"
                                                    Grid.Column="1"
                                                    HorizontalTextAlignment="End"
                                                    Text="Delete"
                                                    TextColor="Red">
                                                    <Label.GestureRecognizers>
                                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:CallbackDetailsViewModel}}, Path=DeleteCommand}" CommandParameter="{Binding .}" />
                                                    </Label.GestureRecognizers>
                                                </Label>

                                            </Grid>
                                        </VerticalStackLayout>
                                    </Grid>
                                </Frame>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </RefreshView>

            <ActivityIndicator
                Grid.RowSpan="2"
                Grid.ColumnSpan="2"
                HorizontalOptions="Fill"
                IsRunning="{Binding IsBusy}"
                IsVisible="{Binding IsBusy}"
                VerticalOptions="Center" />
        </Grid>

    </ScrollView>

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior Command="{Binding GetHistoryCommand}" EventName="Appearing" />
    </ContentPage.Behaviors>

</ContentPage>